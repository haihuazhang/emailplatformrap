projection;
strict ( 2 );
use draft;

define behavior for ZC_ZT_XLS_TEMPLATE alias XLSX
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}